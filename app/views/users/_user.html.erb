<li>
  <%= gravatar_for user, size: Settings.app.models.user.avatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to t(".delete_hyperlink"), user, method: :delete, data: {confirm: t(".confirm_delete_message")} %>
  <% end %>
</li>
